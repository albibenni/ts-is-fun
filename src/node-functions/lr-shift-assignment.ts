let a = 5;
a <<= 1; // equivalent to x = x << 1 | x = 10
console.log(a);

/**
 * Returns a number or BigInt whose binary representation is the frist operand shifted by the specific number of bis to the left.
 * Excess bits shifted of to the left are discarded. Zero bits are shifted in from the right.
 */
